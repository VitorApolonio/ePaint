import { createRoot } from 'react-dom/client';

const AboutWindow = () => {
  return (
    <div id="root">
      <p>Under construction...</p>
    </div>
  );
};

createRoot(document.body).render(
  <AboutWindow />,
);
