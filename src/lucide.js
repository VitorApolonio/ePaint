import { createIcons, Brush, Eraser, Undo, Redo, ArrowLeftRight, BrushCleaning, PaintBucket } from "lucide";

// adds icons to elements with matching 'data-lucide' attribute
createIcons({
  icons: {
    Brush,
    Eraser,
    Undo,
    Redo,
    ArrowLeftRight,
    BrushCleaning,
    PaintBucket
  }
})